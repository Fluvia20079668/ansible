name: 🚀 Terraform + Docker CI/CD to EC2

on:
  push:
    branches: [ "main" ]
    paths:
      - 'terraform/**'
      - 'Dockerfile'
      - '**/*.py'
      - '**/*.js'
      - '**/*.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      AWS_REGION: eu-north-1
      ECR_REPO_NAME: my-simple-app
      IMAGE_TAG: latest
      APP_PORT: 8090

    steps:
      - name: 🧩 Checkout repository
        uses: actions/checkout@v4

      - name: ⚙️ Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.5.7

      - name: 🔑 Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: 🏗️ Terraform Init
        working-directory: terraform
        run: terraform init

      - name: 🌱 Terraform Apply
        working-directory: terraform
        id: tf
        run: terraform apply -auto-approve

      - name: 🌐 Get EC2 Public IP and Private Key
        id: ec2
        working-directory: terraform
        run: |
          echo "EC2_PUBLIC_IP=$(terraform output -raw ec2_public_ip)" >> $GITHUB_ENV
          echo "PRIVATE_KEY_PEM=$(terraform output -raw private_key_pem)" >> $GITHUB_ENV

      - name: 🐳 Log in to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: 🧱 Build, Tag, and Push Docker Image
        run: |
          IMAGE_URI=${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPO_NAME }}:${{ env.IMAGE_TAG }}
          docker build -t $IMAGE_URI .
          docker push $IMAGE_URI
          echo "IMAGE_URI=$IMAGE_URI" >> $GITHUB_ENV

      - name: 🚀 Deploy Docker on EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ env.EC2_PUBLIC_IP }}
          username: ec2-user
          key: ${{ env.PRIVATE_KEY_PEM }}
          port: 22
          script_stop: true
          command_timeout: 30m
          script: |
            sudo dnf install -y docker awscli
            sudo systemctl enable docker
            sudo systemctl start docker
            aws ecr get-login-password --region ${{ env.AWS_REGION }} | sudo docker login --username AWS --password-stdin ${{ steps.login-ecr.outputs.registry }}
            docker stop my-simple-app || true
            docker rm my-simple-app || true
            docker pull ${{ env.IMAGE_URI }}
            docker run -d -p ${{ env.APP_PORT }}:${{ env.APP_PORT }} --name my-simple-app --restart always ${{ env.IMAGE_URI }}

      - name: 🌍 Show live app URL
        run: |
          echo "🎉 Your app is live at: http://${{ env.EC2_PUBLIC_IP }}:${{ env.APP_PORT }}"
